# 宿主机上映射到容器 3000 的端口。只绑 127.0.0.1，供反向代理使用。
APP_PORT=3100

# quiz.db 的宿主机目录。必须是「已存在」且属于 uid 1001 的目录：
#   mkdir -p /www/my_project/pinyin/quiz && chown -R 1001:1001 /www/my_project/pinyin/quiz
DATA_HOST_PATH=/www/my_project/pinyin/quiz
